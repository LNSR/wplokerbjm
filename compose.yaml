services:
  wordpress:
    build: .
    container_name: wordpress
    expose:
      - "9000"
    environment:
      WORDPRESS_DB_HOST: ${WORDPRESS_DB_HOST}
      WORDPRESS_DB_USER: ${WORDPRESS_DB_USER}
      WORDPRESS_DB_PASSWORD: ${WORDPRESS_DB_PASSWORD}
      WORDPRESS_DB_NAME: ${WORDPRESS_DB_NAME}
      WP_ENV: "production"
      WP_REPAIR: "false"
      FORCE_SSL_ADMIN: "true"
      WP_HOME: ${WORDPRESS_WP_HOME}
      WORDPRESS_WP_SITEURL: ${WORDPRESS_WP_SITEURL}      
      WORDPRESS_CONFIG_EXTRA: |
        if (!defined('WP_CACHE')) define('WP_CACHE', true);
        define('WP_CACHE_KEY_SALT', '${WORDPRESS_WP_SITEURL}');
        define('WP_REDIS_HOST', 'redis');
        define('WP_REDIS_PASSWORD', '${REDIS_PASSWORD}');
        define('WP_REDIS_DATABASE', ${REDIS_DATABASE});

        if (!defined('WP_ENV')) {
          define('WP_ENV', getenv('WP_ENV') ?: 'production');
        }

        // Set protocol and hostname
        if (isset($$_SERVER['HTTP_HOST'])) {
          $$hostname = $$_SERVER['HTTP_HOST'];
        } else {
          $$hostname = 'localhost';
        }
        $$protocol = (!empty($$_SERVER['HTTPS']) && $$_SERVER['HTTPS'] !== 'off') ? 'https://' : 'http://';

        // Ngrok/Proxy detection
        if (isset($$_SERVER['HTTP_X_FORWARDED_PROTO']) && strpos($$_SERVER['HTTP_X_FORWARDED_PROTO'], 'https') !== false) {
          $$_SERVER['HTTPS'] = 'on';
          $$protocol = 'https://';
        }
        if (isset($$_SERVER['HTTP_X_FORWARDED_HOST']) && strpos($$_SERVER['HTTP_X_FORWARDED_HOST'], 'ngrok') !== false) {
          $$hostname = $$_SERVER['HTTP_X_FORWARDED_HOST'];
        }

        define('WP_HOME', $$protocol . $$hostname);
        define('WP_SITEURL', $$protocol . $$hostname);

        switch (WP_ENV) {
          case 'development':
            // Debugging
            if (!defined('WP_DEBUG')) define('WP_DEBUG', true);
            if (!defined('WP_DEBUG_LOG')) define('WP_DEBUG_LOG', '/var/www/html/wp-content/debug/debug.log');
            if (!defined('WP_DEBUG_DISPLAY')) define('WP_DEBUG_DISPLAY', true);
            if (!defined('SCRIPT_DEBUG')) define('SCRIPT_DEBUG', true);

            // Performance for dev
            define('WP_POST_REVISIONS', 3);
            define('AUTOSAVE_INTERVAL', 120);
            define('EMPTY_TRASH_DAYS', 7);

            // Cron/Updates
            define('DISABLE_WP_CRON', false);
            define('WP_AUTO_UPDATE_CORE', true);

            // Memory
            define('WP_MEMORY_LIMIT', '1024M');
            define('WP_MAX_MEMORY_LIMIT', '8196M');

            // Dev mode
            define('DISALLOW_FILE_EDIT', false);
            define('DISALLOW_FILE_MODS', false);
            define('WP_ENVIRONMENT_TYPE', 'development');
            define('WP_DEVELOPMENT_MODE', 'all');

            // Asset concatenation/compression
            define('COMPRESS_CSS', false);
            define('COMPRESS_SCRIPTS', false);
            define('CONCATENATE_SCRIPTS', false);
            define('ENFORCE_GZIP', false);
            break;

          case 'production':
          default:
            // Production settings
            if (!defined('WP_DEBUG')) define('WP_DEBUG', false);
            if (!defined('WP_DEBUG_DISPLAY')) define('WP_DEBUG_DISPLAY', false);
            if (!defined('SCRIPT_DEBUG')) define('SCRIPT_DEBUG', false);
            define('DISALLOW_FILE_EDIT', true);
            define('DISALLOW_FILE_MODS', true);
            define('WP_ENVIRONMENT_TYPE', 'production');
            define('FORCE_SSL_ADMIN', true); // Force SSL for admin
            define('AUTOSAVE_INTERVAL', 300); // Autosave every 5 minutes
            define('EMPTY_TRASH_DAYS', 7); // Empty trash every week
            define('WP_POST_REVISIONS', 10); // Limit post revisions
            define('WP_MEMORY_LIMIT', '2048M'); // Set memory limit
            define('WP_MAX_MEMORY_LIMIT', '8196M'); // Set max memory limit
            define('WP_CRON_LOCK_TIMEOUT', 60); // Cron lock timeout
            define('WP_AUTO_UPDATE_CORE', false); // Disable core auto-updates
            define('DISABLE_WP_CRON', true); // Use system cron instead
            define('CONCATENATE_SCRIPTS', true); // Combine JS files
            define('COMPRESS_SCRIPTS', true); // Compress JS
            define('COMPRESS_CSS', true); // Compress CSS

            // Security hardening
            define('DISALLOW_UNFILTERED_HTML', true);
            define('WP_HTTP_BLOCK_EXTERNAL', true);
            define('WP_ACCESSIBLE_HOSTS', 'api.wordpress.org'); // Allow updates

            // Prevent file editing and modifications
            define('FS_METHOD', 'direct');
            break;
        }

        // Allow DB repair if enabled
        define('WP_ALLOW_REPAIR', filter_var(getenv('WP_REPAIR'), FILTER_VALIDATE_BOOLEAN) ? true : false);
    volumes:
      - wordpress:/var/www/html
      - ./wordpress:/var/www/html/wp-content
      - ./wordpress/themes/astra-child/node_modules:/var/www/html/node_modules
      - ./.htaccess:/var/www/html/.htaccess
    depends_on:
      - db
    healthcheck:
      test: ["CMD", "curl", "-f", "https://localhost"]
      interval: 1m
      timeout: 10s
      retries: 3
    extra_hosts:
      - "host.docker.internal:host-gateway"

  caddy:
    image: caddy:latest
    container_name: caddy
    depends_on:
      - wordpress
    ports:
      - "443:443/udp"
      - "443:443/tcp"
      - "80:80"
    volumes:
      - wordpress:/var/www/html:ro
      - ./wordpress:/var/www/html/wp-content:ro
      - ./wordpress/themes/astra-child/node_modules:/var/www/html/node_modules:ro
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      # generate your own SSL
      - ./localhost.pem:/var/www/certs/localhost.crt:ro
      - ./localhost-key.pem:/var/www/certs/localhost.key:ro
      # Optional: persist Caddy data
      - caddy_data:/data
      - caddy_config:/config

  db:
    image: mysql:8.0
    container_name: wordpress_db
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - db:/var/lib/mysql
    ports:
      - "3306:3306"
    healthcheck:
      test:
        [
          "CMD",
          "mysqladmin",
          "ping",
          "-h",
          "localhost",
          "-u$$MYSQL_USER",
          "-p$$MYSQL_PASSWORD",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
  phpmyadmin:
    restart: always
    image: phpmyadmin
    container_name: phpmyadmin
    environment:
      PMA_HOST: ${PMA_HOST}
      PMA_USER: ${PMA_USER}
      PMA_PASSWORD: ${PMA_PASSWORD}
      PMA_ARBITRARY: 1
      APACHE_PORT: 81
    ports:
      - "81:81"
    depends_on:
      - db
  redis:
    image: redis:alpine
    container_name: wordpress_redis
    command: redis-server --requirepass ${REDIS_PASSWORD} --maxmemory ${REDIS_MAXMEMORY} --maxmemory-policy ${REDIS_MAXMEMORY_POLICY}
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
  # ngrok:
  #   image: ngrok/ngrok:latest
  #   container_name: ngrok
  #   restart: unless-stopped
  #   command: http wordpress:80
  #   environment:
  #     - NGROK_AUTHTOKEN=${NGROK_AUTH_TOKEN}
  #   ports:
  #     - "4040:4040"
  #   depends_on:
  #     - wordpress

volumes:
  wordpress:
  db:
  redis_data:
  caddy_data:
  caddy_config:
